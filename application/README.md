# application

The application under test: a small life-insurance web application (Python/Flask + SQL database) supporting login, policy and customer search, policy create/update, premium calculation, status changes, validation errors, and two user roles.
