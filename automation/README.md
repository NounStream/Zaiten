# automation

Linux-executable automation framework built with Playwright + Chromium, deliberately structured to mirror UFT concepts: a locator map acting as an object repository, reusable functions, data-driven input, and a keyword dispatcher. Serves as the design prototype for the UFT implementation in ../uft/.
