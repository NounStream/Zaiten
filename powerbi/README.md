# powerbi

Quality-reporting assets: test-results CSV extracts and screenshots of the Power BI dashboard (total/passed/failed/blocked, pass rate, defects by severity and status, duration, failure category, trend). Power BI Desktop runs on Windows.
