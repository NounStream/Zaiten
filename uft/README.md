# uft

UFT One artifacts produced on an authorized Windows environment: GUI tests, shared and local object repositories, VBScript function libraries, keyword tables, and execution evidence. The installed UFT version is documented here once installation is complete. UFT does not run on Linux; see the root README.
